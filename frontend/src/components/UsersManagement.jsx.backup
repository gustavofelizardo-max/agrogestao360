import React, { useState } from 'react';
import { useLanguage } from '../context/LanguageContext';
import { User, Plus, Edit, Trash2, Mail } from 'lucide-react';

const UsersManagement = () => {
  const { t } = useLanguage();
  const [users] = useState([
    {
      id: 1,
      name: 'Gustavo Felizardo',
      email: 'gufeliza@gmail.com',
      userType: 'producer',
      createdAt: '2024-01-15'
    }
  ]);

  return (
    <div style={{ padding: '25px', fontFamily: 'Arial, sans-serif' }}>
      <div style={{ 
        display: 'flex', 
        justifyContent: 'space-between', 
        alignItems: 'center',
        marginBottom: '30px'
      }}>
        <h2 style={{ 
          color: '#1e293b', 
          margin: 0, 
          fontSize: '28px', 
          fontWeight: '600',
          display: 'flex',
          alignItems: 'center',
          gap: '12px'
        }}>
          <User size={32} color="#3b82f6" />
          {t('users.title') || 'Gerenciamento de Usuários'}
        </h2>
        
        <button 
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: '8px',
            backgroundColor: '#3b82f6',
            color: 'white',
            border: 'none',
            padding: '12px 20px',
            borderRadius: '8px',
            cursor: 'pointer',
            fontSize: '14px',
            fontWeight: '500'
          }}
        >
          <Plus size={16} />
          {t('users.new') || 'Novo Usuário'}
        </button>
      </div>

      {users.length === 0 ? (
        <div style={{
          backgroundColor: '#fff',
          padding: '60px',
          borderRadius: '12px',
          boxShadow: '0 4px 6px rgba(0, 0, 0, 0.05)',
          border: '1px solid #e2e8f0',
          textAlign: 'center'
        }}>
          <User size={64} color="#d1d5db" style={{ marginBottom: '20px' }} />
          <h3 style={{ color: '#64748b', marginBottom: '15px', fontSize: '20px' }}>
            {t('users.empty.title') || 'Nenhum usuário cadastrado'}
          </h3>
          <p style={{ color: '#94a3b8', marginBottom: '20px', fontSize: '16px' }}>
            {t('users.empty.description') || 'Cadastre usuários para gerenciar acessos'}
          </p>
          <button
            style={{
              backgroundColor: '#3b82f6',
              color: 'white',
              border: 'none',
              padding: '12px 30px',
              borderRadius: '8px',
              cursor: 'pointer',
              fontSize: '14px',
              fontWeight: '500'
            }}
          >
            {t('users.empty.button') || 'Cadastrar Primeiro Usuário'}
          </button>
        </div>
      ) : (
        <div style={{ 
          display: 'grid', 
          gridTemplateColumns: 'repeat(auto-fill, minmax(320px, 1fr))', 
          gap: '20px' 
        }}>
          {users.map((user) => (
            <div
              key={user.id}
              style={{
                backgroundColor: '#fff',
                padding: '20px',
                borderRadius: '12px',
                boxShadow: '0 4px 6px rgba(0, 0, 0, 0.05)',
                border: '1px solid #e2e8f0',
                transition: 'transform 0.2s, box-shadow 0.2s'
              }}
              onMouseOver={(e) => {
                e.currentTarget.style.transform = 'translateY(-2px)';
                e.currentTarget.style.boxShadow = '0 8px 12px rgba(0, 0, 0, 0.1)';
              }}
              onMouseOut={(e) => {
                e.currentTarget.style.transform = 'translateY(0)';
                e.currentTarget.style.boxShadow = '0 4px 6px rgba(0, 0, 0, 0.05)';
              }}
            >
              <div style={{ 
                display: 'flex', 
                justifyContent: 'space-between', 
                alignItems: 'flex-start', 
                marginBottom: '15px' 
              }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: '10px' }}>
                  <User size={20} color="#3b82f6" />
                  <h3 style={{ 
                    margin: 0, 
                    color: '#1e293b', 
                    fontSize: '18px', 
                    fontWeight: '600' 
                  }}>
                    {user.name}
                  </h3>
                </div>
                
                <div style={{ display: 'flex', gap: '8px' }}>
                  <button
                    style={{
                      backgroundColor: '#3b82f6',
                      color: 'white',
                      border: 'none',
                      padding: '8px',
                      borderRadius: '6px',
                      cursor: 'pointer',
                      display: 'flex',
                      alignItems: 'center'
                    }}
                    title={t('common.edit') || 'Editar'}
                  >
                    <Edit size={14} />
                  </button>
                  
                  <button
                    style={{
                      backgroundColor: '#ef4444',
                      color: 'white',
                      border: 'none',
                      padding: '8px',
                      borderRadius: '6px',
                      cursor: 'pointer',
                      display: 'flex',
                      alignItems: 'center'
                    }}
                    title={t('common.delete') || 'Excluir'}
                  >
                    <Trash2 size={14} />
                  </button>
                </div>
              </div>

              <div style={{ marginBottom: '12px', display: 'flex', alignItems: 'center', gap: '8px' }}>
                <Mail size={16} color="#64748b" />
                <p style={{ 
                  color: '#64748b', 
                  fontSize: '14px', 
                  margin: 0
                }}>
                  {user.email}
                </p>
              </div>

              <div style={{ marginBottom: '12px' }}>
                <span style={{ 
                  backgroundColor: user.userType === 'producer' ? '#dcfce7' : 
                               user.userType === 'supplier' ? '#dbeafe' : '#fef3c7',
                  color: user.userType === 'producer' ? '#166534' : 
                         user.userType === 'supplier' ? '#1e40af' : '#92400e',
                  padding: '4px 12px', 
                  borderRadius: '20px', 
                  fontSize: '12px',
                  fontWeight: '500'
                }}>
                  {user.userType === 'producer' ? 
                    (t('user.types.producer') || 'Produtor') :
                    user.userType === 'supplier' ? 
                    (t('user.types.supplier') || 'Fornecedor') : 
                    (t('user.types.buyer') || 'Comprador')
                  }
                </span>
              </div>

              <p style={{ 
                color: '#94a3b8', 
                fontSize: '12px', 
                margin: 0
              }}>
                Criado em: {new Date(user.createdAt).toLocaleDateString('pt-BR')}
              </p>
            </div>
          ))}
        </div>
      )}
    </div>
  );
};

export default UsersManagement;
